# 从推理模型到 RSI：AI 能力增长范式的演化

> 日期：2026-08-11
>
> 主题：LLM → Reasoning Model → Agentic System → Recursive Self-Improvement（RSI）

## 一、今天讨论的核心问题

今天的讨论最终聚焦到一个比“Agent 是否是下一代 AI”更根本的问题：

> **AI 的能力究竟通过什么机制持续增长？当前是否已经出现了新的能力增长范式？**

为了回答这个问题，需要严格区分两种完全不同的“范式”：

1. **应用范式**：模型被怎样使用，例如 Chat、Reasoning、Agent。
2. **能力增长范式**：模型或 AI 系统为什么会变得更强，新的 intelligence 从哪里来。

如果不区分这两者，很容易把“Agent 能完成更复杂任务”误认为“Agent 已经改变了智能增长机制”。

---

## 二、从普通 LLM 到 Reasoning Model，确实是一次重要范式转变

### 1. 底层架构没有发生根本替换

Reasoning Model 仍然通常建立在 autoregressive Transformer 上：

```text
context
  ↓
next token
  ↓
new context
  ↓
next token
  ↓
...
```

因此，Reasoning Model 并不是因为突然出现了一个独立的“逻辑 CPU”才获得推理能力。

真正重要的是：

> **模型被训练成能够利用生成出来的中间 token 作为计算状态，形成长程 reasoning trajectory。**

例如：

```text
提出假设
  ↓
推导
  ↓
发现矛盾
  ↓
否定假设
  ↓
尝试新路径
  ↓
验证
  ↓
最终答案
```

这些 reasoning token 不只是给人阅读的文字，它们生成后会重新进入上下文，改变后续计算。

因此可以把 CoT 理解成一种外显的计算轨迹，而不是简单的“模型像人一样在脑子里想”。

### 2. Reasoning Model 引入了新的 scaling 轴

传统能力提升主要依赖：

```text
更多参数
+ 更多数据
+ 更多训练 FLOPs
      ↓
更强模型
```

Reasoning Model 增加了一个非常重要的维度：

```text
同一个模型
+ 更多 inference-time compute
      ↓
更长 reasoning trajectory
+ 更多探索 / 验证 / 修正
      ↓
更高问题解决能力
```

也就是说，**训练完成以后仍然可以通过增加 test-time compute 来系统性换取更强能力**。

这使 Reasoning Scaling 不只是一个新的应用方式，而是一个真实的新能力扩展机制。

### 3. 训练对象也从“答案”逐渐转向“产生成功轨迹的策略”

普通监督学习更像：

```text
Question → Answer
```

而 reasoning RL 更接近：

```text
Problem
  ↓
Reasoning trajectory
  ↓
Answer
  ↓
Verifier / Reward
  ↓
更新生成 reasoning trajectory 的 policy
```

模型学习的不只是答案本身，还包括：

- 如何分解问题；
- 如何提出假设；
- 如何验证中间结果；
- 如何发现矛盾；
- 如何回退；
- 如何更换路线；
- 什么时候继续计算；
- 什么时候停止。

这也是为什么 Reasoning Model 可以被视为一次明显的能力增长范式变化。

---

## 三、Agentic 目前首先是一种系统 / 应用范式，而不一定是新的能力增长范式

Agent 把模型放进了一个外部反馈闭环：

```text
State
  ↓
Reason
  ↓
Action
  ↓
Environment
  ↓
Observation
  ↓
New State
  ↺
```

模型不再只是产生答案，而是通过工具调用改变环境状态。

这使研究对象从：

```text
Question → Answer
```

变成：

```text
Goal → Trajectory → Outcome
```

这是巨大的系统范式变化，但必须继续追问：

> **究竟是模型本身变聪明了，还是系统给模型增加了更多外部能力？**

例如：

```text
LLM + Calculator
```

会显著提高数学任务表现，但不能因此认为模型自身的数学智能发生了根本突破。

同样：

```text
Reasoning Model
+ Browser
+ Terminal
+ Memory
+ Harness
```

能够完成数小时的 coding task，首先证明的是 **system capability** 提升，而不自动证明模型能力增长机制发生了改变。

因此：

> **Agentic 是非常重要的应用和系统范式，但目前尚不能仅凭 Agent 的兴起，就断言基础模型已经进入了与 Reasoning Scaling 同等清晰的新能力增长范式。**

---

## 四、Agentic Trace 很重要，但“更多 Agent Trace”本身还不是根本变化

Agentic trajectory 可以表示为：

```text
Goal
 ↓
S0
 ↓
A0
 ↓
O0
 ↓
S1
 ↓
A1
 ↓
O1
 ↓
...
 ↓
Outcome
 ↓
Reward
```

这类数据越来越适合训练：

- tool use；
- failure recovery；
- replanning；
- long-horizon decision making；
- termination；
- environment adaptation。

但如果过程仍然只是：

```text
收集更多 trajectory
  ↓
离线训练下一版模型
```

那么本质上仍然可以理解为“新的训练数据来源”。

真正可能形成新范式的地方，是下面这个闭环：

```text
模型进入环境
  ↓
自主行动
  ↓
获得 experience
  ↓
发现 failure
  ↓
生成训练信号
  ↓
更新模型 / skill / harness / verifier
  ↓
能力提升
  ↓
进入更困难环境
  ↺
```

此时 scaling 的对象开始从 **data** 变成 **experience**。

---

## 五、可能的新能力增长轴：Experience Scaling

一个值得持续研究的假设是：

> **Experience 是否会成为继 Data、Training Compute、Test-time Compute 之后的新 Scaling Axis？**

可以把几代 scaling 粗略表示为：

### 第一代：Pretraining Scaling

```text
Data
+ Parameters
+ Training Compute
      ↓
Capability
```

### 第二代：Reasoning Scaling

```text
RL / Verifiable Reward
+ Test-time Compute
      ↓
Reasoning Capability
```

### 第三代候选：Experience Scaling

```text
Environment
  ↓
Action
  ↓
Feedback
  ↓
Experience
  ↓
Learning
  ↓
Stronger Policy
  ↓
Harder Environment
  ↺
```

目前第三个循环正在被大量探索，但还没有像前两个一样被证明能够稳定、长期、开放式地 scaling。

---

## 六、RSI 不一定表现为“模型直接修改自己的权重”

传统对 Recursive Self-Improvement（RSI）的想象往往是：

```text
Model₀
  ↓
修改自己的代码 / 权重
  ↓
Model₁
  ↓
再次修改自己
  ↓
Model₂
  ↓
...
```

这是一种最强、最直接的 RSI，但并不是唯一可能形式。

关键问题应该改成：

> **“self”究竟指什么？**

如果 `self = 单个模型实例`，那么今天显然还没有实现完整 autonomous RSI。

如果：

```text
self = 模型 + Agent Harness + 数据系统 + Eval + 训练系统 + AI 研发流程
```

那么递归自我改进可能已经以更分布式、更间接的形式开始出现。

---

## 七、RSI 可能具有多种形态

### R0：Self-Refinement

改进当前答案或 reasoning trajectory：

```text
Output
 ↓
Critique
 ↓
Revision
```

这是最弱的一层递归。

### R1：Scaffold / Harness RSI

```text
Agent
 ↓
执行任务
 ↓
Trace / Failure
 ↓
修改 Prompt / Skill / Memory / Tool / Harness
 ↓
更强 Agent
 ↺
```

模型权重不变，但整个智能系统变强。

### R2：Data / Evaluator / Training RSI

```text
Model₀
 ↓
生成题目 / trajectory / verifier
 ↓
筛选训练数据
 ↓
训练 Model₁
 ↓
Model₁ 生成更高质量训练信号
 ↓
训练 Model₂
 ↺
```

这里被递归改进的是训练数据和训练过程。

### R3：Training-System RSI

AI 可以帮助改进：

- CUDA kernel；
- 编译器；
- 分布式训练；
- RL pipeline；
- 数据管线；
- hyperparameter；
- training recipe；
- evaluation infrastructure。

于是出现：

```text
Model₀
 ↓
改进训练基础设施
 ↓
更高效地训练 Model₁
 ↓
Model₁ 更擅长改进训练系统
 ↓
Model₂
 ↺
```

### R4：AI-R&D RSI

模型开始参与创造下一代模型：

```text
Modelₙ
 ↓
提出 hypothesis
 ↓
写实验代码
 ↓
运行实验
 ↓
分析结果
 ↓
改进架构 / 算法 / recipe
 ↓
Modelₙ₊₁
```

如果 `Modelₙ₊₁` 又比 `Modelₙ` 更擅长 AI R&D，那么递归项开始真正闭合。

### R5：Organization-Level RSI

现实中最可能首先出现的 RSI 可能不是一个完全自主的模型，而是：

```text
Human Researchers
      ↕
AI Research Agents
      ↕
Experiments / Evals / Data / Compute
      ↕
Next-generation Models
```

整个 AI 实验室逐渐成为一个人类 + AI + 计算 + 实验反馈组成的复合自改进系统。

---

## 八、判断 RSI 是否真正成立的核心：递归项是否闭合

单纯：

```text
AI → 帮助研究员写代码
```

不能称为 RSI。

真正关键的是：

```text
AIₙ
 ↓
提高 AI R&D 能力
 ↓
产生 AIₙ₊₁
 ↓
AIₙ₊₁ 比 AIₙ 更擅长提高 AI R&D
 ↓
更快 / 更有效地产生 AIₙ₊₂
 ↺
```

形式上可以写成：

```text
Cₙ₊₁ = f(Cₙ)
```

其中 `C` 最关键的不是一般任务能力，而是：

> **Capability to Improve AI——改进 AI 的能力。**

只有当“改进能力本身也随着迭代被持续改进”时，RSI 才真正具有递归意义。

---

## 九、RSI 已经开始 ≠ Intelligence Explosion 已经开始

即使递归反馈存在：

```text
AIₙ
 ↓
AI R&D
 ↓
AIₙ₊₁
```

也不意味着能力必然指数爆炸。

它仍然可能被以下因素限制：

- compute；
- 芯片与能源；
- 数据；
- verifier；
- 实验周期；
- 架构瓶颈；
- 训练稳定性；
- 人类决策；
- 现实世界反馈延迟。

递归增益可能是：

```text
10% → 8% → 5% → 2% → ...
```

而不是：

```text
10% → 30% → 100% → 1000%
```

因此必须严格区分：

1. **存在递归反馈；**
2. **递归反馈能够持续 scaling；**
3. **出现开放式自我改进；**
4. **出现 intelligence explosion。**

这四者不是同一件事。

---

## 十、对当前阶段的工作假设

今天讨论形成的一个较为稳健的判断是：

```text
Pretraining Scaling
        ↓
Reasoning Scaling
        ↓
AI-assisted AI R&D
        ↓
Early / Distributed RSI   ← 当前可能正在这里
        ↓
Autonomous RSI            ← 尚未证明
        ↓
Open-ended RSI            ← 尚未证明
        ↓
Intelligence Explosion ?
```

因此，比“现在是不是 Agent 时代”更值得追踪的问题是：

> **每一代 AI 是否越来越擅长完成创造下一代 AI 所需要的工作？**

进一步：

> **这种能力是否正在持续缩短 AI R&D 周期、提高实验吞吐量、改善训练方法，并产生复合回报？**

如果答案持续为是，那么 RSI 就不再只是未来概念，而会成为 AI 能力增长的真实动力机制。

---

## 十一、这对 Agent / AI 研究方向的启示

这一框架与“封闭、可验证环境中的自我循环”天然连接。

真正值得研究的不只是：

> 如何做一个更复杂的 Agent？

而是：

> **能否构造一个封闭、可验证、可测量的环境，使 Agent 反复经历 Experience → Failure → Feedback → Modification → Evaluation，并证明系统能力随循环稳定提升？**

可以把研究对象定义成：

```text
Agent₀
 ↓
执行任务
 ↓
Trace
 ↓
Failure Diagnosis
 ↓
Modification
 ↓
Agent₁
 ↓
Benchmark
 ↓
能力是否真的提高？
 ↺
```

关键不是某次修改成功，而是：

1. 是否能够自动发现能力缺陷；
2. 是否能够形成正确 failure hypothesis；
3. 是否能够提出有效 intervention；
4. 是否能够通过 benchmark 证明改进；
5. 是否能够把成功经验固化为 skill / policy / memory / training data；
6. 下一轮是否因此能解决以前无法解决的问题；
7. 这种提升是否能够持续多轮，而不是很快饱和。

这实际上可以成为一个小规模、工程化、可验证的 RSI 研究框架。

---

## 十二、今天最重要的几个结论

### 结论 1

**Reasoning Model 是一次真实的能力增长范式变化。**

因为它不仅改变了使用方式，还引入了 test-time compute scaling，并通过 outcome-driven reasoning training 学习更有效的计算轨迹。

### 结论 2

**Agentic 目前首先是应用 / 系统范式。**

Agent 能力显著增强，并不自动意味着模型自身的能力增长机制发生了根本变化。

### 结论 3

**Agentic trace 的真正价值不只是作为更多训练数据，而是可能成为 experience-driven learning 的原材料。**

当 Experience → Learning → Stronger Agent → Better Experience 形成闭环时，才可能出现新的 scaling 机制。

### 结论 4

**RSI 不应被狭义理解成“单个模型修改自己的权重”。**

它可以发生在：

- output；
- harness；
- skill；
- memory；
- data；
- verifier；
- training pipeline；
- AI research；
- 整个 AI R&D organization。

### 结论 5

**我们可能已经处于 Early / Distributed RSI，而不是 Autonomous RSI。**

AI 已经开始帮助创造更好的 AI，但完整自主、开放式、持续复合增长的 RSI 尚未得到证明。

### 结论 6

判断 RSI 的最重要指标不是：

> “AI 会不会修改自己的代码？”

而是：

> **“每一代 AI 是否越来越擅长提高下一代 AI 的能力？”**

### 结论 7

一个非常值得长期追踪的问题是：

> **Experience 能否成为继 Data、Training Compute、Test-time Compute 之后的新 Scaling Axis？**

这可能是 Reasoning Model 之后，比“Agent 是不是下一范式”更加本质的问题。

---

## 十三、后续值得继续研究的问题

1. 如何给“AI 改进 AI 的能力”建立可操作的 benchmark？
2. 如何区分一次性 self-refinement 与真正 recursive improvement？
3. 如何测量多轮改进中的边际收益是否衰减？
4. 哪些任务适合构造封闭、可验证的 RSI 实验环境？
5. Agent 的哪些组成部分最适合自动改进：Prompt、Skill、Memory、Tool、Harness 还是 Model Weight？
6. Verifier 是否是自我改进系统的核心瓶颈？
7. Failure Diagnosis 能否成为自进化 Agent 的基础能力？
8. 能否让 Agent 自动生成针对自身弱点的 curriculum？
9. Agentic experience 能否稳定转化为模型权重中的长期能力？
10. AI-assisted AI R&D 在什么条件下会从“生产力工具”跨越为真正的 RSI？

---

## 一句话总结

> **LLM 时代主要依靠更多数据与训练算力获得能力；Reasoning 时代增加了 test-time compute；Agent 把推理接入了环境反馈闭环；而真正可能定义下一阶段的，不只是 Agent，而是 AI 是否能够把自己的经验、失败和研发能力重新反馈进产生下一代 AI 的过程——也就是不同形态的 Recursive Self-Improvement。**
