# Agent 能力分解与最小能力测试方法论

> 记录时间：2026-08-10  
> 主题：如何从 Agent 设想反推 LLM 所需基础能力，并用最小、透明、可验证的环境测出能力边界，再据此设计 Harness。

## 1. 起点：不要直接问“这个 Agent 能不能做成”

当我们设想一种具有某种宏观能力的 Agent，例如：

- 长程自主研究 Agent；
- 大型软件迁移 Agent；
- 能持续数小时乃至数天工作的 Coding Agent；
- 自主探索软件系统全部功能的 Agent；
- 能完成复杂策略任务的通用任务 Agent；

最常见的工程路径是立即开始搭系统：Planner、Memory、Tool、Reflection、Verifier、Sub-agent、Workflow……然后找几个复杂任务直接测试。

这种方法最大的问题是：**复杂系统失败以后，我们往往不知道失败发生在哪一层。**

一个大型 Agent 任务失败，可能是：

- 模型没有理解目标；
- 模型无法稳定维护状态；
- 模型只能做局部贪心决策；
- 模型无法形成多步策略；
- 模型无法发现子目标；
- 模型无法识别死路；
- 模型无法从失败中恢复；
- 模型不知道什么时候应该重新规划；
- 外部 Memory 设计有问题；
- Planner 产生了不可执行的计划；
- Tool interface 造成信息损失；
- Verifier 没有及时反馈；
- Termination 条件错误；
- Harness 把原本简单的问题复杂化了。

因此，更科学的问题应该是：

> **如果这个 Agent 真的能够完成我们设想的宏观任务，那么底层模型至少必须具备哪些基础能力？**

然后继续问：

> **这些基础能力能否在最简单、最透明、最容易验证的环境中被独立测量？**

这构成了一个比“直接构造 Agent”更基础的方法论。

---

## 2. 核心链条：从宏观 Agent 反推基础能力

我们可以把 Agent 研究写成如下依赖链：

```text
Agent Idea
    ↓
Task Structure Analysis
    ↓
Required Model Capabilities
    ↓
Minimal Capability Tests
    ↓
Measured Capability Boundary
    ↓
Harness Design
    ↓
System Evaluation
```

也就是：

> **Agent 设想 → 任务结构 → 必要能力 → 最小能力测试 → 能力边界 → Harness 设计 → 系统评估**

这是一个非常关键的转变。

过去的思路往往是：

```text
我想做一个很强的 Agent
        ↓
堆 Planner / Memory / Tools / Reflection
        ↓
看最终效果好不好
```

新的思路是：

```text
我想做一个很强的 Agent
        ↓
这个目标在逻辑上依赖哪些能力？
        ↓
当前 LLM 是否真的拥有这些能力？
        ↓
能力极限在哪里？
        ↓
Harness 应该补偿什么，而不是凭经验堆什么？
```

这里产生了一个重要关系：

```text
Agent Architecture
        ↓
Required Model Capabilities
```

不同 Agent 架构，对模型基础能力的要求完全不同。

例如：

### 架构 A：长程 open-loop planning

```text
一次生成完整 50 步计划
        ↓
连续执行
        ↓
最后统一验证
```

它要求模型具有很强的：

- 长程规划；
- 内部状态模拟；
- 长期一致性；
- 对远期动作后果的预测。

### 架构 B：短周期闭环控制

```text
规划 3～5 步
    ↓
执行
    ↓
观察真实状态
    ↓
验证
    ↓
重新规划
```

它对 50 步内部规划能力的要求很低，却高度依赖：

- 当前状态理解；
- 局部策略；
- 反馈利用；
- 重新规划；
- 错误恢复；
- 外部状态管理。

因此，不应该笼统地问：

> “LLM 是否足够强？”

而应该问：

> **“这个 Agent 架构假设模型具有什么能力，而这些能力当前是否真实存在？”**

---

## 3. 长程任务真正要求什么？

直觉上，一个模型如果完全不具有长程规划能力，就很难可靠完成真正的长程任务。

但这里需要做一个更精确的区分：

> **长程任务必然要求某种跨时间保持目标并产生有效行为的能力，但不一定要求模型一开始生成完整的长程计划。**

一个系统完全可以这样工作：

```text
观察
→ 局部规划
→ 执行动作
→ 获得反馈
→ 更新状态
→ 重新规划
→ ……
```

最终走完 1000 步。

它可能从来没有在任何一个时刻生成过完整的 1000 步计划。

但是，它仍然必须拥有若干基础能力，例如：

- 长期目标保持；
- 当前状态与目标之间关系的判断；
- 局部动作后果预测；
- 子目标发现；
- 避免循环；
- 必要时接受短期退步；
- 检测错误；
- 从错误恢复；
- 根据新信息改变策略；
- 识别何时应该终止。

因此，我们真正应该测的，不只是一个笼统的“长程规划”标签，而是构成长程智能的一组基础能力。

---

## 4. 最重要的 Benchmark 原则：约束接口，而不是约束认知

Benchmark 的一个核心原则应该是：**透明性。**

Benchmark 负责规定：

- 模型能看到什么；
- 模型能输出什么；
- 模型能调用什么工具；
- 环境会提供什么反馈；
- 允许多少 token / compute / sampling；
- 什么状态算成功；
- 如何评分。

但是 Benchmark 不应该规定模型内部必须如何思考。

可以把原则概括为：

> **Constrain the interface, not the cognition.**  
> **约束接口，而不是约束认知。**

例如，我们要测试纯模型的华容道规划能力，可以规定：

```text
Input:
    一个确定的华容道棋盘状态

Allowed:
    模型自身的上下文与内部计算

Forbidden:
    Python
    BFS/A* 外部求解器
    网络
    外部 memory
    中间环境反馈

Output:
    一次性完整动作序列

Budget:
    固定 token / compute budget

Evaluation:
    动作是否合法
    是否到达目标
    路径长度
    与最优解的差距
```

至于模型内部实际上使用：

- 模式识别；
- 隐式搜索；
- learned heuristic；
- internal recurrence；
- latent planning；
- 某种近似 A*；
- 或未来我们根本不了解的新型内部算法；

都不应该成为 Benchmark 的限制对象。

如果能力发生在**模型内部**，那就是模型能力的一部分。

真正需要隔离的是**模型外部的 scaffolding**。

例如：

```text
Model A
Input → Model → Output
```

与：

```text
Model B
Input
  ↓
Model
  ↓
Python BFS
  ↓
Model
  ↓
Output
```

如果 Benchmark 声称测的是 model intelligence，这两种实验条件就不能混为一谈。

因此：

> **Benchmark 应该把测试对象的边界定义清楚，而不是试图规定测试对象内部的思维过程。**

---

## 5. “不管内部细节”不等于“不管实验条件”

这一点尤其重要。

我们可以完全把模型视为黑盒，但模型外部的实验条件必须极其透明。

假设一个结果写着：

```text
Model X：华容道成功率 83%
```

如果不知道它是：

- 一次性输出完整方案；
- 还是每走一步重新观察棋盘；
- 是否允许调用 Python；
- 是否允许 BFS/A*；
- 是否允许外部 Memory；
- 是否允许 100 次 sampling 再用 verifier 选最优；
- 是否有 human feedback；
- 是否使用专项 RL；

那么这个 83% 基本没有可解释性。

所以 Benchmark 的透明性应该集中在：

- **Input transparency**；
- **Tool transparency**；
- **Feedback transparency**；
- **Budget transparency**；
- **Sampling transparency**；
- **Scoring transparency**。

而模型内部可以完全黑盒。

最终只研究：

```text
Task Conditions
        ↓
Behavioral Performance
```

这是一种行为主义式的能力测量。

---

## 6. 为什么华容道、Sokoban、Rush Hour 这类环境特别重要

华容道本身并不是因为“游戏重要”才有研究价值。

它的重要性来自它可以隔离很多基础能力，同时又具备：

- 确定性；
- 完全可观测；
- 状态转移明确；
- 动作合法性可验证；
- 最优解可以由传统 solver 精确求出；
- 可以系统控制任务难度；
- 几乎不依赖专业知识；
- 可以避免现实任务中的大量噪声。

这使它成为研究 **模型自身策略能力** 的一个很干净的实验环境。

### 6.1 状态追踪

移动一个方块后，模型是否知道所有方块现在在哪里？

如果几十步之后状态表征已经漂移，那么长程决策必然受到影响。

### 6.2 动作后果模拟

给定：

```text
State S + Action A
```

模型能否正确预测：

```text
Next State S'
```

这是最基本的 world-model 能力。

### 6.3 非贪心策略

华容道中经常需要：

> 暂时把目标方块向“错误方向”移动，才能为以后创造空间。

这可以测试模型是否拥有：

- 延迟收益；
- 短期负收益容忍；
- 长程因果意识；
- 非局部策略。

### 6.4 子目标发现

模型可能需要意识到：

```text
真正的问题暂时不是移动曹操
        ↓
先移动其他方块
        ↓
腾出关键空间
        ↓
构造新的局面
        ↓
最终目标才可达
```

这就是 primitive subgoal discovery。

### 6.5 必要回退

某些局面要求：

```text
看起来离目标更远
        ↓
但实际上是唯一正确路径
```

这可以专门测：

> **模型是否能够接受局部恶化，以换取全局可达性？**

这类能力与很多真实长程 Agent 任务高度同构。

---

## 7. Minimal Capability Witness：最小能力见证

我们可以正式引入一个概念：

> **Minimal Capability Witness（最小能力见证）**

含义是：

> 构造一个尽可能简单的任务，使得成功完成它至少需要我们想验证的某种能力，同时尽量消除其他能力、知识和工具的干扰。

例如我们要测试：

> 模型是否具有“为了长期目标接受短期退步”的能力？

根本不需要先让它迁移一个百万行软件项目。

可以设计一个只有几个状态的环境：

```text
              +1
START ─────────────→ A ───→ DEAD END
  │
  │ -1
  ↓
  B ───→ C ───→ GOAL +10
```

如果模型稳定选择眼前 +1，它连最基本的非贪心策略都没有表现出来。

这种任务的价值在于：

- 规则简单；
- 没有领域知识；
- 没有复杂工具；
- 没有海量状态；
- 失败原因高度可解释。

这比直接在复杂任务中观察“Agent 好像没有战略性”科学得多。

---

## 8. Capability Unit Tests：给 Agent 做能力单元测试

软件工程不会只写一个：

```text
test_entire_system()
```

然后成功就算成功，失败就不知道为什么。

成熟工程一定会有：

```text
test_parser()
test_state_machine()
test_retry()
test_scheduler()
test_database()
...
```

Agent 研究也应该如此。

现在很多 Agent benchmark 更像：

```text
test_entire_agent()
```

例如：

- 完成这个 Web task；
- 修复这个 GitHub issue；
- 完成这个软件项目；
- 执行一次复杂研究任务。

Agent 失败以后，我们很难知道到底是哪种基础能力先崩溃。

因此，可以建立：

> **Capability Unit Tests for Agents**

即：**Agent 基础能力单元测试。**

---

## 9. 一个可能的 Agent 基础能力层级

### Level 0：状态理解

输入：

```text
State S
```

测试：

> 模型能否正确理解当前世界？

主要测：representation / grounding。

---

### Level 1：一步状态预测

输入：

```text
State S + Action A
```

输出：

```text
Predicted State S'
```

主要测：局部 world model。

---

### Level 2：一步决策

```text
State S + Goal G
        ↓
Best Next Action
```

主要测：局部 policy。

---

### Level 3：短程规划

要求模型规划：

```text
3～5 steps
```

主要测：浅层 search / planning。

---

### Level 4：非贪心策略

任务被专门设计成：

```text
局部最优动作
    ↓
必然失败

短期退步
    ↓
最终成功
```

主要测：真正意义上的 strategy。

---

### Level 5：长程规划

逐渐提高最优解深度：

```text
5
10
20
30
50
80
100
...
```

然后观察模型性能如何退化。

这可以定义一个非常重要的概念：

> **Effective Planning Horizon（有效规划视界）**

即：在给定成功率阈值和资源预算下，模型能够可靠处理的最大规划深度。

---

### Level 6：动态重规划

在执行过程中改变环境：

```text
Original Plan
      ↓
Unexpected State Change
      ↓
New Observation
      ↓
Replan
```

主要测：adaptation / replanning。

---

### Level 7：失败恢复

故意把模型放进一个已经犯错的状态：

```text
Error State
    ↓
Detect
    ↓
Diagnose
    ↓
Rollback / Recover
    ↓
Choose New Strategy
```

主要测：error recovery。

---

### Level 8：长期自主循环

最后才进入真正复杂的 Agent loop：

```text
Build
→ Observe
→ Diagnose
→ Modify
→ Evaluate
→ ...
```

此时，我们已经拥有一张相对清晰的基础能力地图，而不再只是盯着最终 success rate。

---

## 10. 不只看 success rate，而要测 capability surface

假设我们用华容道或其他可精确求解环境，可以控制：

- 最优解深度；
- branching factor；
- dead-end density；
- 必要回退长度；
- 状态空间大小；
- 局部诱惑强度；
- 子目标数量；
- 解的冗余度。

那么可以建立：

```text
Task Difficulty Dimensions
          ↓
Model Behavioral Performance
```

而不是只得到一个简单的：

```text
Success Rate = 63%
```

例如，我们可以画：

```text
Success
100% ┤██████████
     │████████
     │██████
     │████
     │██
  0% ┼──────────────────────
       5 10 20 30 50 80 100
          Optimal Depth
```

并记录：

- 首次错误发生在哪一步；
- 非法动作率；
- 是否重复访问状态；
- 是否进入循环；
- 是否识别死局；
- 错误以后能否恢复；
- 每一步离最优目标是更近还是更远；
- 路径长度与最优路径差距；
- 推理 token 随难度的增长曲线。

最终得到的是模型的：

> **Capability Surface（能力曲面）**

而不是一个单点分数。

---

## 11. Benchmark 可以反过来指导 Harness 设计

这是整个方法论最有工程价值的一步。

假设测试得到：

```text
5-step planning:   98%
10-step planning:  91%
20-step planning:  55%
40-step planning:  12%
```

那么一个需要高可靠性的 Agent 架构，就不应该默认：

```text
让模型一次规划 50 步
然后连续执行
```

而应该主动把控制周期缩短，例如：

```text
3～8 步规划
    ↓
Checkpoint
    ↓
Verifier
    ↓
Observe Real State
    ↓
Replan
```

于是 Harness 不再是：

> “大家都说 Memory / Reflection / Planner 有用，所以我也加上。”

而变成：

> **“我们已经测到了模型在哪些能力上存在边界，因此系统结构专门补偿这些边界。”**

这可以写成：

```text
Measured Model Weakness
        ↓
Architectural Compensation
```

例如：

| 测得的模型缺陷 | Harness 补偿方式 |
|---|---|
| 长程状态容易漂移 | 外部状态存储 / environment feedback |
| 20 步以上 open-loop planning 不可靠 | 缩短 planning horizon，频繁 replan |
| 容易重复访问状态 | trajectory memory / visited-state tracking |
| 死局识别能力弱 | verifier / solver-assisted dead-end detector |
| 错误后恢复能力弱 | checkpoint / rollback |
| 子目标发现不稳定 | hierarchical planner / task decomposition |
| 终止判断不稳定 | explicit termination verifier |

这样一来，Harness 设计从“经验主义堆组件”转变为“针对测得能力缺口进行工程补偿”。

---

## 12. 必要能力与充分能力必须区分

这一方法论中还有一个重要逻辑边界。

假设模型在纯内部环境中：

> 20 步确定性规划表现很差。

这**不能严格推出**：

> 它一定无法完成任何 500 步 Agent 任务。

因为 Agent 可能拥有：

- 环境反馈；
- 外部 Memory；
- verifier；
- checkpoint；
- replanning；
- solver；
- human-in-the-loop。

这些机制都可以把一个长程任务转化为大量短闭环任务。

但这个测试仍然能够告诉我们：

> **如果某个 Agent 架构依赖模型独立完成 20～50 步内部规划，那么这个架构的基础假设可能就是错误的。**

因此：

- Capability test 通常测的是**必要能力或能力边界**；
- 它不一定直接给出整个 Agent 的充分条件；
- 复杂 Agent 能否成功，取决于模型能力与 Harness 的组合。

可以写成：

```text
Agent Capability
    ≠
Model Capability Alone
```

更接近：

```text
Agent Capability
    =
Model Capabilities
+
Harness Structure
+
Tools
+
Environment Feedback
+
Memory
+
Verification
+
Control Policy
```

因此最终研究目标不是只给模型打分，而是建立：

> **模型能力边界与 Agent 架构之间的映射关系。**

---

## 13. 一个更加科学的 Agent Engineering 循环

这套思想最后可以形成一个完整闭环：

```text
1. 提出 Agent 能力设想
        ↓
2. 分析任务结构
        ↓
3. 推导必需的模型基础能力
        ↓
4. 为每种能力设计最小可验证环境
        ↓
5. 测量当前 LLM 的能力边界
        ↓
6. 根据能力边界设计 Harness
        ↓
7. 在组合系统上做端到端测试
        ↓
8. 观察失败 trajectory
        ↓
9. 判断失败来自模型还是系统
        ↓
10. 修改架构或重新定义能力要求
        ↓
循环
```

这与此前形成的 Agent 学习闭环高度一致：

```text
Build
→ Observe
→ Diagnose
→ Modify
→ Evaluate
```

但在 Build 之前增加了一层更加基础的工作：

```text
Capability Hypothesis
→ Minimal Test
→ Capability Boundary
```

于是完整流程变成：

> **Capability Hypothesis → Minimal Environment → Behavioral Test → Capability Boundary → Harness Design → Build → Observe → Diagnose → Modify → Evaluate**

---

## 14. 这可能是创造不同 Agent 的共同理论基础

不同 Agent 表面上差异巨大：

- Coding Agent；
- Research Agent；
- Web Agent；
- Data Agent；
- Migration Agent；
- Game Agent；
- Autonomous Exploration Agent；
- Long-horizon Project Agent。

但从底层看，它们都可以被分析成：

```text
宏观任务要求
    ↓
基础认知能力组合
    ↓
模型当前能力边界
    ↓
Harness 对不足能力的外部补偿
    ↓
最终系统能力
```

所以一种可能的 Agent Engineering 理论框架是：

> **Agent 不是“给 LLM 加工具”这么简单，而是根据目标任务所需能力，对 Model 与 Harness 进行能力分工与结构组合。**

Harness 的意义也因此被重新定义：

> **Harness 是对模型能力边界的结构性补偿。**

如果模型未来变强：

- 某些 Planner 会消失；
- 某些 Memory 机制会简化；
- 某些 verifier 需求会降低；
- 某些复杂 workflow 会被模型自身能力吸收。

而新的 Agent 设计空间，又会出现在模型尚未具备的能力边界上。

因此，研究模型缺陷并不是负面的事情。

恰恰相反：

> **模型能力缺陷定义了 Agent Engineering 当前最有价值的设计空间。**

---

## 15. 最终可以浓缩成几条原则

### 原则一：先分析能力依赖，再实现 Agent

不要先堆系统，再解释为什么有效。

先问：

> 这个 Agent 成立，模型至少必须会什么？

---

### 原则二：基础能力应在最小环境中验证

不要用百万行项目测试一个原子认知能力。

能用十个状态测清楚的问题，就不要用复杂现实环境制造噪声。

---

### 原则三：Benchmark 约束接口，不约束认知

> **Constrain the interface, not the cognition.**

模型内部如何做到，是模型自己的事情。

---

### 原则四：Benchmark 外部条件必须完全透明

输入、工具、反馈、预算、sampling、评分方式必须公开。

---

### 原则五：复杂 Agent 需要 Capability Unit Tests

不能只有 `test_entire_agent()`。

必须知道系统依赖哪些基础能力，以及哪一种能力首先失效。

---

### 原则六：测量能力曲线，而不是只测一个分数

真正重要的是：

> 随着任务难度增加，模型在什么位置开始退化？

这定义了能力边界。

---

### 原则七：Harness 应该针对已测得的模型缺陷设计

不是为了“Agent 看起来更复杂”而添加组件。

而是：

```text
Model Weakness
    ↓
Harness Compensation
```

---

### 原则八：区分模型能力与 Agent 系统能力

一个 Agent 成功，不等于底层模型拥有全部能力；
一个纯模型测试失败，也不等于 Agent 系统一定无法完成长程任务。

真正需要研究的是两者之间的组合关系。

---

## 16. 一个值得继续研究的核心问题

这套方法最终指向一个更大的研究问题：

> **我们能否建立一套“Agent 基础能力科学”，像软件工程中的单元测试、材料工程中的材料参数、控制工程中的系统响应一样，在构造 Agent 之前先测量 LLM 的能力边界，再据此设计系统？**

如果可以，那么 Agent Engineering 就会从：

> 凭经验试 Prompt、Memory、Planner、Reflection

逐渐走向：

> **测量 → 建模 → 设计 → 验证。**

这可能成为创造各种不同 Agent 的一个共同理论基础。

---

## 17. 一句话总结

> **先证明模型具备构成目标能力所需的最小原子能力，再研究这些能力如何通过 Harness 被组合、补偿和放大，最终成长为长程 Agent 能力。**

或者更加工程化地说：

> **Capability Hypothesis → Minimal Test → Capability Boundary → Harness Design → System Evaluation.**
